# Custom Countdown

## Scenario
You are adding a settings page to your application; to improve the UX (user experience), you wish to add a countdown timer to revert the original configuration in case a user unknowingly makes a change that would be difficult to undo later. For example, changing your screen's resolution.


Your settings screen looks like this:
```rust
{{#rustdoc_include custom_countdown_example/settings.rs}}
```

## Solution


## Using Tasks Instead